version: '3.8'

services:
  backend:
    build: .
    ports:
      - "5000:5000"   # Cambia este puerto si usas otro en tu app
    env_file:
      - .env
    volumes:
      - .:/app
    command: python3 app.py   # Cambia este comando según tu punto de entrada